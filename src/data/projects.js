export const projects = {
memora: {
  gradient: 'cover-gradient-1',
  coverImage: '/covers/memora-cover.png',
  title: 'Memora',
  status: { cls: 'status-live', label: 'Live' },
  eyebrow: 'Social \u00b7 Real-time',
  explore: 'Read the case study \u2192',
  summary: 'A full-stack real-time memory vault and social journaling platform in which diary entries, photo albums, time capsules, and live chat, all synced instantly across devices.',
  tags: ['React 19', 'Node.js', 'Express 5', 'MongoDB', 'Mongoose', 'Socket.io', 'JWT', 'Cloudinary', 'Passport.js', 'Tailwind CSS', 'Framer Motion', 'Vite'],
  overview: 'Memora is a production-grade MERN application for personal and shared memory-keeping. Users log diary entries and photo albums, build time capsules that stay cryptographically locked until a future date, journal collaboratively inside group diaries, and communicate through a fully real-time chat system with typing indicators, read receipts, and presence tracking. The backend is a modular Express 5 API secured with httpOnly JWT cookies and silent access-token refresh, backed by MongoDB Atlas via Mongoose, with a dedicated Socket.io layer handling messaging, friend requests, and live notifications. The frontend is a React 19 + Vite SPA with over 40 components, custom hooks for socket state, and Framer Motion-driven page/modal transitions.',
  features: [
    'Dual-cookie JWT authentication — short-lived 15-minute access tokens paired with 7-day refresh tokens, both httpOnly/SameSite-protected, with an Axios interceptor that queues concurrent requests during silent token refresh and only logs the user out on a confirmed 401 (never on network blips)',
    'Google OAuth 2.0 sign-in via Passport.js, auto-provisioning accounts and de-duplicating by email or Google ID',
    'Time capsules with three lock states (Locked / Pending / Sent), enforced future unlock dates, private vs. shared visibility, granular viewer/collaborator permissions, and server-side gating so locked content is never sent to the client before its unlock date',
    'Secret diary entries protected by a bcrypt-hashed PIN, independent of the account password, plus opt-in burn-after-reading entries that self-delete after a shared recipient\'s first read',
    'Group diaries with invite-code-based membership, admin roles, and per-group shared journaling feeds',
    '"On This Day" nostalgia engine that resurfaces past entries from the same calendar day in prior years, enriched with contextual mood-based messaging',
    'Streak-tracking system that computes consecutive-day journaling streaks server-side, correctly handling same-day re-entries, streak breaks, and longest-streak records',
    'Real-time chat over raw Socket.io (not a library wrapper) with an in-memory presence map, typing indicators scoped to the correct conversation, delivery + read receipts, and inline sharing of capsules or memories as rich chat bubbles',
    'Live friend-request flow — requests, accepts, and declines are persisted to MongoDB and pushed instantly over sockets to both parties without a page refresh',
    'Signed, backend-brokered Cloudinary uploads (separate signature endpoints for images vs. voice notes) so API secrets never reach the client, plus client-side canvas-based image compression with binary-search quality tuning to hit target file sizes before upload',
    'Voice-note diary entries with duration metadata and dedicated Cloudinary storage',
    'Mood tracking with a unique-per-day compound index preventing duplicate mood logs',
    'Interactive memory calendar, monthly recap slideshow, and dashboard carousel built with Framer Motion for fluid, app-like transitions',
  ],
  arch: 'The backend is an Express 5 REST API organized into isolated route/controller/model layers (auth, capsules, diary entries, photo albums, group diaries, comments, friends, messages, mood). Authentication uses JWT access + refresh tokens stored in httpOnly cookies rather than localStorage, mitigating XSS token theft, with CORS configured to explicitly whitelist credentialed origins. A dedicated Socket.io module (config/socket.js) runs alongside the HTTP server on the same Node process, maintaining an in-memory userId→socketId presence map to route direct messages, typing events, and friend-request notifications without polling. MongoDB (via Mongoose) models capture polymorphic relationships , e.g. capsules reference either DiaryMemory or PhotoAlbum documents through a refPath-based discriminator and enforce data integrity with compound unique indexes (mood-per-day) and cascading authorization checks (ownership vs. shared-access vs. public) evaluated per-request in controllers. Media is never proxied through the Node server: the client requests a short-lived Cloudinary upload signature from the backend, uploads directly to Cloudinary, and only the resulting secure URL is persisted thhus keeping the API stateless and bandwidth-light. The React 19 frontend is built with Vite, uses a single shared Axios instance with request/response interceptors to handle silent token refresh and request queueing, and organizes real-time state (chat, presence, notifications) through socket event listeners wired into page-level components. Tailwind CSS v4 and Framer Motion drive a cohesive, animated design system across 40+ components and 15+ routed pages.',
  github: 'https://github.com/Summiedev/memora',
  live: 'https://me-m-ora.onrender.com/',
},
  verdikt: {
    gradient: 'cover-gradient-2',
    coverImage: '/covers/verdikt-cover.png',
    title: 'Verdikt',
    status: { cls: 'status-beta', label: 'Beta' },
    eyebrow: 'Social \u00b7 Real-time',
    explore: 'Read the case study \u2192',
  summary: 'A real-time multiplayer social polling game built for group sessions — players vote, react, and watch live results update in sync.',
  tags: ['Spring Boot', 'WebSocket', 'React', 'TypeScript', 'PostgreSQL', 'DigitalOcean', 'STOMP', 'Docker'],
  overview: 'Verdikt is a full-stack real-time social game where groups join live sessions, respond to curated relationship and lifestyle questions, and see results update instantly across all clients. The app combines social interaction, real-time synchronization, and session-based gameplay into a polished multiplayer experience.',
  features: [
    'Real-time room orchestration using Spring Boot and STOMP over WebSocket',
    'Live vote casting and synchronized result aggregation across all players',
    'Host-controlled lobby flow with room lifecycle, question progression, and session end states',
    'Curated question bank with category-based content organization',
    'End-of-session report card generation for results and winners',
    'PostgreSQL-backed persistence for rooms, players, questions, and vote history',
    'React and TypeScript frontend with event-driven live UI updates',
    'Containerized deployment strategy suitable for DigitalOcean App Platform and managed PostgreSQL',
  ],
  arch: 'The backend is built with Spring Boot and uses STOMP over WebSocket for low-latency event delivery. Game state is maintained server-side to ensure consistency across concurrent voters, while PostgreSQL stores room data, player sessions, question content, and vote history. The frontend uses React with TypeScript and reactive socket hooks to keep the user interface synchronized with the live game state. The system is designed to be horizontally scalable and deployment-ready for DigitalOcean.',
    github: 'https://github.com/Summiedev/verdikt', 
  live: 'https://verdikt.online',
  },
  dilamme: {
  gradient: 'cover-gradient-3',
  coverImage: '/covers/dillamme-scheduler-cover.png',
  title: 'Dilamme Scheduler',
  status: { cls: 'status-live', label: 'Live' },
  eyebrow: 'Infrastructure \u00b7 Internship build',
  explore: 'Read the case study \u2192',
  summary: 'A distributed background job scheduler built from scratch — custom priority-queue and timing-wheel engine, DAG dependency chains, retry/DLQ handling, and live status updates over Server-Sent Events.',
  tags: ['Python', 'FastAPI', 'MongoDB', 'Redis', 'React', 'TanStack Query', 'Tailwind CSS', 'Server-Sent Events', 'PM2', 'Nginx'],
  overview: 'Dilamme Scheduler is a production-style job orchestration system engineered around two custom-built data structures rather than an off-the-shelf queue library: a lazy-deletion indexed priority queue and a circular timing wheel. Jobs are submitted through a FastAPI REST API, persisted in MongoDB, and executed by a fully independent worker process that the API never imports or calls directly so the two processes coordinate exclusively through MongoDB state and Redis pub/sub, communicating architecture that mirrors real distributed-systems design rather than a toy scheduler. The system supports DAG-based job dependencies with cycle detection, exponential backoff with jitter, dead-letter queuing with alert thresholds, deterministic idempotent recurring jobs, and a starvation-prevention SLA that mathematically guarantees no job waits more than 30 minutes regardless of priority. All of it is observable live in a React dashboard that streams state transitions via Server-Sent Events with automatic reconnect and resync.',
  features: [
    'Custom IndexedPriorityQueue (min-heap over a 3-field sort key: effective_priority → scheduled_at → created_at) with O(log n) in-place priority updates via lazy deletion where stale heap entries are skipped on pop rather than removed eagerly, with automatic full-heap rebuilds once dead entries exceed max(32, live_count)',
    'Custom TimingWheel algorithm implemented is a 3,600-slot circular array (one slot per second, 1-hour horizon) that promotes due jobs to the heap in O(k) per tick instead of pushing every future job into the heap immediately',
    'Starvation-prevention algorithm with a proven SLA: age-proportional priority boosting guarantees any job pending 30+ minutes is promoted to an effective_priority below 1.0, jumping ahead of every unaged high-priority job — boost formula and thresholds fully derived and documented',
    'DAG-based job dependencies with compile-time cycle detection (DFS traversal on job creation, returns the exact cycle path in the 400 response) and terminal-state propagation that fails downstream jobs immediately if an upstream dependency fails or is cancelled',
    'Defence-in-depth duplicate-execution protection: MongoDB atomic find_one_and_update claims (status transition IS the lock) layered with a Redis SET NX EX distributed lock, plus background TTL lock-extension for long-running jobs and heartbeat-based stuck-job recovery every 5 minutes',
    'Exponential backoff with jitter across 3 retry attempts (1s → 5s → 25s, ±jitter) before a job is embedded with a dlq subdocument in-place thus no separate DLQ collection, so retries are a single atomic $unset/$set operation',
    'DLQ threshold alerting (10 failed jobs) with a 1-hour cooldown to prevent alert storms',
    'Idempotent recurring jobs using deterministic UUID v5 IDs derived from the parent job ID and next-run timestamp, so a worker crash mid-rollover cannot create duplicate recurring chains',
    'Cooperative cancellation model for in-flight jobs (cancel_requested flag checked post-handler) — an explicit, documented tradeoff over unsafe coroutine termination, preserving external-system consistency',
    'Real-time dashboard via Server-Sent Events: worker publishes state changes to a Redis job_events channel, FastAPI fans them out per-client through asyncio.Queue-backed SSE streams, and the frontend reconnects with exponential backoff and force-refetches all active TanStack Query caches on reconnect to guarantee zero missed updates',
    'Five pluggable job handler types (send_email, webhook, log_processing, generate_report, upload_file) with per-type payload validation',
    'Structured JSON logging (structlog) queryable through a dedicated /api/logs endpoint, filterable by job_id and event type',
    'Built-in benchmark suite comparing the custom heap, the timing-wheel pipeline, and indexed vs. non-indexed MongoDB queries at 10k–50k document scale, with published P50/P95/stddev numbers',
  ],
  arch: 'Two independent OS processes — a FastAPI/Uvicorn API and a standalone asyncio worker — run under PM2 and never import each other; all coordination happens through MongoDB (source of truth for job state, compound-indexed on status/effective_priority/scheduled_at/created_at) and Redis (pub/sub for live events, NX locks for execution safety). The worker loop ticks every 2 seconds: it advances the timing wheel to promote due jobs, polls MongoDB for anything scheduled but not yet in memory, runs the starvation check every 60s and stuck-job recovery every 5 minutes, then drains the in-memory heap — for each job running a DAG dependency check, an atomic MongoDB claim, a Redis lock acquisition, handler execution, and a success/retry/DLQ resolution path. State changes are published to Redis and bridged to browser clients over Server-Sent Events (dedicated Nginx location block with buffering disabled, since standard proxy buffering silently breaks SSE delivery). The React frontend uses TanStack Query for server-state caching and a custom SSE manager with reconnect-and-resync logic, giving the dashboard, jobs table, and DLQ view live updates with no polling. The whole stack — Nginx, FastAPI, worker, MongoDB, Redis and is deployed to a single VPS behind PM2 process management with a documented, git-cloneable deploy script.',
  github: 'https://github.com/Summiedev/dillamme-scheduler',
  live: 'https://dillema.duckdns.org',
},
  procurement: {
    gradient: 'cover-gradient-4',
    title: 'Procurement Intelligence',
    status: { cls: 'status-live', label: 'Live' },
    eyebrow: 'Freelance \u00b7 Automation',
    explore: 'Read the case study \u2192',
    summary: 'Automated BOQ and cable schedule processing for electrical infrastructure procurement.',
    tags: ['Python', 'openpyxl', 'XLSX', 'Data processing'],
    overview: 'A set of document processing tools built for electrical engineering procurement teams. Converts formal quotations into structured XLSX files with live formulas, VAT calculations, and aggregated cable quantity reports.',
    features: [
      'Automated XLSX generation from raw quotation data',
      'Live VAT formula calculation with dynamic totals',
      'Cable schedule aggregation across multiple BOQ files',
      'Branded document styling per vendor',
      'Structured procurement tables for cable quantities',
      'Tolerant data extraction from inconsistent sources',
    ],
    arch: 'A Python pipeline using openpyxl for XLSX generation. Input data is parsed, validated, and mapped to structured schemas before being written to formatted spreadsheets with embedded formulas. Supports batch processing across vendor quotations.',
    github: 'https://github.com/',
    live: null,
  },
  feasibility: {
    gradient: 'cover-gradient-5',
    title: 'Software Feasibility Engine',
    status: { cls: 'status-archived', label: 'Consulting' },
    eyebrow: 'Consulting \u00b7 Strategy',
    explore: 'Read the case study \u2192',
    summary: 'Technical and business assessment framework for 14 software ideas in the Nigerian market.',
    tags: ['System design', 'Paystack', 'Termii', 'Flutterwave'],
    overview: 'A consulting engagement evaluating 14 software project ideas for a client deciding what to build next. Each idea was assessed for technical feasibility, delivery timeline, market fit, and recommended Nigerian-specific tech stack.',
    features: [
      'Technical feasibility scoring across 14 concepts',
      'Nigeria-specific stack recommendations',
      'Delivery timeline estimates with risk factors',
      'Market fit analysis for local digital infrastructure',
      'Regulatory and compliance notes',
      'Architecture sketches for prioritised candidates',
    ],
    arch: 'A consistent evaluation framework: problem definition, market size, technical complexity, team requirements, stack compatibility, and phased delivery milestones, weighted toward low-cost infrastructure where it made sense.',
    github: null,
    live: '#',
  },
  portfolio: {
    gradient: 'cover-gradient-6',
    title: 'Portfolio Platform',
    status: { cls: 'status-live', label: 'Live' },
    eyebrow: 'Mentorship',
    explore: 'View details \u2192',
    summary: 'Building portfolio sites alongside emerging Nigerian backend developers entering the global market.',
    tags: ['HTML/CSS', 'JavaScript', 'UX design', 'Mentorship'],
    overview: 'A mentorship effort helping emerging backend developers build portfolio sites that actually communicate their engineering ability. The work covered UX, frontend implementation, and positioning.',
    features: [
      'Hover-to-reveal project card interactions',
      'Responsive navigation with scroll state',
      'Accessible light theme with proper contrast',
      'Performance-optimised CSS animations',
      'Copy guidance for project framing',
      'GitHub profile and presentation coaching',
    ],
    arch: 'Plain HTML, CSS, and vanilla JavaScript, no build tooling, so developers early in their careers can own and extend the sites themselves.',
    github: null,
    live: '#',
  },
}

